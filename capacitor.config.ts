import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aplic.vivencias',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
